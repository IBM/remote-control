# Prompts

This directory holds records of AI prompts used when using coding assistants during development. Each time a PR is added that was built using one or more session prompts, a new file should be added here with the initial prompt. The intent is not to capture _all_ interactions (full traces), but rather to capture the initial prompts that were used to start significant pieces of work.