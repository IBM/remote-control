# Compiled binaries
remote-control
remote-control-android
*.test

# Coverage outputs
coverage.out
coverage.html

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# We use out-of-source beads tracking, so any local .beads is ephemeral
.beads

# We use lots of agents, but don't commit to any
.bob
.claude
.opencode

# Don't commit any scratch from tmp
tmp

# Local environment configuration
.env